[package]
name = "arena-sim"
edition.workspace = true
version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic fixed-timestep FPS simulation: movement, collision, ballistics, damage."

[dependencies]
arena-protocol = { path = "../arena-protocol" }
arena-content = { path = "../arena-content" }
arena-mythos = { path = "../arena-mythos" }
# For build_zone_geometry: turn the content worldgen recipe into a zone's collider.
# arena-procgen depends only on protocol+content, so this is acyclic.
arena-procgen = { path = "../arena-procgen" }
glam.workspace = true
serde.workspace = true
tracing.workspace = true
sha2.workspace = true

[dev-dependencies]
bincode.workspace = true
